/*
 * File: app/controller/WinAddRowController.js
 *
 * This file was generated by Sencha Architect version 4.2.2.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.5.x Classic library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.5.x Classic. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('A_AR_RECEIPT.controller.WinAddRowController', {
	extend : 'Ext.app.Controller',

	control : {
		"#addRowBtn" : {
			click : 'addRowBtnClick'
		}
	},

	addRowBtnClick : function(button, e, eOpts) {
		var store1 = Ext.getStore('MyStore1');
		var store2 = Ext.getStore('MyStore2');
		var count = store2.getCount();
		var rowCount = Number(Ext.getCmp('rowCount').getValue());
		var AR_NO = '';

		for (var i = 0; i < rowCount; i++) {
			var SEQ = store1.data.length+store2.data.length+1;
			store1.each(function(record,idx) {
				if(idx == 0) AR_NO = record.get('AR_NO');
				
				if(Number(record.get('SEQ')) >= SEQ){
					SEQ = Number(record.get('SEQ'))+1;
				}
			});
			store2.each(function(record,idx) {
				if(Number(record.get('SEQ')) >= SEQ){
					SEQ = Number(record.get('SEQ'))+1;
				}
			});
			
			count = count + 1;
			var rec = Ext.create('A_AR_RECEIPT.model.MyModel', {
				V_TYPE: 'I',
				SEQ: SEQ,
				AR_NO: AR_NO,
				DEPT_CD: Ext.getCmp('V_DEPT_CD2').getValue(),
				DEPT_NM: Ext.getCmp('V_DEPT_CD2').getValue(),
				CUR: Ext.getCmp('V_CUR').getValue(),
				XCH_RATE: 1,
				CLS_TYPE: 'Z',
			});
			store2.insert(count - 1, rec);
		}

		var win = Ext.WindowManager.getActive();
		if (win) {
			win.close();
		}
	}

});
