/*
 * File: app/controller/TbButtonController.js
 *
 * This file was generated by Sencha Architect version 4.2.4.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.5.x Classic library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.5.x Classic. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('st_pay_reg.controller.TbButtonController', {
    extend: 'Ext.app.Controller',

    stores: [
        'MyStore',
        'MyStore1',
    ],

    control: {
        "#selBtn": {
            click: 'selBtnClick'
        },
        "#saveBtn": {
            click: 'saveBtnClick'
        },
        "#delBtn": {
            click: 'delBtnClick'
        },
        "#clsBtn": {
            click: 'clsBtnClick'
        },
        "mysearchform textfield[name=search_field]": {
            specialkey: 'tfEnter'
        }
    },

    selBtnClick: function(button, e, eOpts) {
    	var store = this.getMyStoreStore();
    	store.removeAll();
    	store.load({
    		params: {
    			V_TYPE: 'SH',
    			V_COMP_ID : parent.Ext.getCmp('GCOMP_ID').getValue(),
    			V_USR_ID : parent.Ext.getCmp('GUSER_ID').getValue(),
    			V_GR_DT_FROM : Ext.getCmp('V_GR_DT_FROM').getValue(),
    			V_GR_DT_TO : Ext.getCmp('V_GR_DT_TO').getValue(),
    			V_M_BP_NM : Ext.getCmp('V_M_BP_NM').getValue(),
    			V_M_BP_CD : Ext.getCmp('V_M_BP_CD').getValue(),
    			V_JIGUB_DT : Ext.getCmp('V_JIGUB_DT').getValue(),
    		},
    		callback: function(records,operations,success){
    		}
    	})
    },

    saveBtnClick: function(button, e, eOpts) {
    	var store = Ext.getStore('MyStore');
		var store1 = Ext.getStore('MyStore1');
		var gridRecord = Ext.getCmp('myGrid').getSelectionModel().getSelection();
		var flag = '';
		var msg = '';
		
		var V_JIGUB_DT = Ext.getCmp('V_JIGUB_DT').getValue();
		
		if(gridRecord.length < 1){
			flag = 'F';
			msg = '저장할 항목을 선택해주세요.'
		}
		else if(V_JIGUB_DT == '' || V_JIGUB_DT == null || V_JIGUB_DT == undefined){
			flag = 'F';
			msg = '지급일자를 선택해주세요.'
		}
		else{
			flag = 'T';
		}
		
		for(var i = 0 ; i < gridRecord.length ; i ++){
			if(gridRecord[i].get('V_TYPE') == 'V'){
				gridRecord[i].set('V_TYPE', 'I');
			}
			if(gridRecord[i].get('JIGUB_AMTS')<= 0){
				flag = 'F';
				msg = '지급금액을 확인해주세요.'
			}
		}
		
		if(flag == 'T'){
			
			Ext.Msg.confirm('확인', '지급전표발행 하시겠습니까?', function(button) {
				if (button == 'yes') {
					store.sync({ 
						params : {
							V_TYPE : 'SYNC',
							V_COMP_ID : parent.Ext.getCmp('GCOMP_ID').getValue(),
							V_USR_ID : parent.Ext.getCmp('GEMP_NO').getValue(),
							V_DEPT_CD : Ext.getCmp('V_DEPT_CD').getValue(),
							V_JIGUB_DT : Ext.getCmp('V_JIGUB_DT').getValue(),
						},
						callback : function(batch) {
				    		var tbController = st_pay_reg.app.getController("TbButtonController");
				    		tbController.selBtnClick();
				    		
				    		try{
				    			var jsonResult = Ext.JSON.decode(batch.operations[0]._response.responseText);
				    			var tryCnt = jsonResult.tryCnt;
				    			var finCnt = jsonResult.finCnt;
				    			var resMSG = jsonResult.resMSG;
				    			var resDATE = jsonResult.resDATE;
				    			var resString = jsonResult.resString;
				    			var TEMP_GL_NO = jsonResult.TEMP_GL_NO;
				    			
				    			if(resMSG == 'SUCCESS') {
				    				Ext.toast({
				    					title : ' ',
				    					timeout : 1000,
				    					html : '전표생성완료',
				    					style : 'text-align: center',
				    					align : 't',
				    					width : 130,
				    				});
				    			}
				    			else{
				    				Ext.Msg.alert('확인', '전표생성실패<br>' + resString);
				    			}
				    		}
				    		catch (e){
				    			myMask.hide();
				    			Ext.Msg.alert('확인', '전표생성실패<br>' + batch.operations[0]._response.responseText);
				    		}
						}, 
					});
				}
			});
		}
		else{
			Ext.Msg.alert('확인', msg);
		}
    },

    delBtnClick: function(button, e, eOpts) {
        alert('delete');
    },

    clsBtnClick: function(button, e, eOpts) {
        var tab=parent.Ext.getCmp('myTab');
        var activeTab=tab.getActiveTab();
        var tabIndex=tab.items.indexOf(activeTab);
        tab.remove(tabIndex,true);
    },

    tfEnter: function(field, e, eOpts) {
        	if (e.getKey() == e.ENTER) {
        		this.selBtnClick();
        	}
    }

});
